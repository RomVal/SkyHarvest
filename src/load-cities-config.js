import { cities } from './default-cities-config.js';

/**
 * Loads the configuration for cities.
 *
 * @returns {Object} The cities configuration.
 */
export const loadCitiesConfig = () => {
  return cities;
};
